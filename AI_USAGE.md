# AI 使用说明

## 使用的工具

- OpenAI Codex：需求拆解、前后端实现、测试和文档初稿。
- OpenAI 内置图像生成：Mira 三态角色板、雨夜咖啡馆环境图。

## AI 参与的工作

- 将六页测试题收敛为可在 72 小时内稳定交付的核心闭环。
- 生成原创成年角色和场景素材。
- 实现状态机、结构化场景指令、浏览器语音、请求取消、迟到响应防护、Mock 服务端和静态降级。
- 优化首次使用路径，包括输入即时回显、慢响应反馈、上下文选项、焦点管理和首屏资源体积。
- 编写单元测试并进行桌面与移动端浏览器检查。

## 关键验证

- 单元测试验证照片、等待对象和离场分支，以及指令协议字段。
- 浏览器逐项验证文字输入、照片事件、角色三态、模拟语音打断、断线、重试与运行日志。
- 在 `1280x720` 和 `390x844` 视口测量页面边界，确认没有横向溢出；检查浏览器控制台无错误。
- 以首次使用者路径检查空输入、发送确认、慢响应、剧情选项变化和诊断抽屉键盘状态。
- 对照原题六页逐项建立需求追踪表；演示视频经过多时间点抽帧、时长、尺寸和音视频编码检查。
- 真实麦克风权限和中文音色仍取决于评审设备；自动化验收覆盖权限不可用时的文字降级和统一取消路径。

## 一次发现并修正的问题

第一次图像生成被要求输出透明角色板，但结果仍带有棕色渐变背景。如果直接叠放会出现明显矩形边缘。实现没有假设 AI 输出符合要求，而是将角色板限制为固定裁切层，并使用边缘遮罩、场景暗部、冷暖光和状态动画融合；随后在桌面与手机截图中检查角色边缘。这个修正也促使角色资产与环境资产保持分离，后续可以无缝替换为真正透明的 Live2D/Rive 资源。

## 人工边界

AI 没有替候选人虚构项目经历、客户结果或个人身份。真实麦克风听感、最终录屏表达和对外提交内容需要候选人本人最后确认。
